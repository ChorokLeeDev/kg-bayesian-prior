% UAI 2026 submission
\documentclass{uai2026} % for initial submission
%\documentclass[accepted]{uai2026} % after acceptance

%% Language
\usepackage[american]{babel}

%% Bibliography
\usepackage{natbib}
    \bibliographystyle{plainnat}
    \renewcommand{\bibsection}{\subsubsection*{References}}

%% Math and formatting
\usepackage{mathtools} % amsmath with fixes and additions
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage{bm}
\setlength{\emergencystretch}{1em}
\hbadness=10000
\vbadness=10000
\tolerance=3000
\emergencystretch=3em

% Theorem environments
\newtheorem{proposition}{Proposition}
\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{remark}{Remark}

% Custom commands
\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathcal{N}}
\newcommand{\GP}{\mathcal{GP}}
\newcommand{\KL}{\text{KL}}
\newcommand{\ELBO}{\mathcal{L}}
\newcommand{\vect}[1]{\bm{#1}}
\newcommand{\mat}[1]{\mathbf{#1}}
\newcommand{\freq}{\text{freq}}

\title{Why Relation-Agnostic Uncertainty Fails: Decomposing OOD Detection in Knowledge Graphs}

\author[1]{Anonymous Author(s)}
\affil[1]{Anonymous Institution}

\begin{document}
\maketitle

\begin{abstract}
\begingroup\sloppy
\input{sections/abstract_uai}
\endgroup
\end{abstract}

\section{Introduction}
\label{sec:intro}
\begingroup\sloppy
\input{sections/introduction_uai}
\endgroup

\section{Related Work}
\label{sec:related}
\begingroup\sloppy
\input{sections/related_work_uai}
\endgroup

\section{Background}
\label{sec:background}
\begingroup\sloppy
\input{sections/background}
\endgroup

\section{Method}
\label{sec:method}
\begingroup\sloppy
\input{sections/method_uai_v2}
\endgroup

\section{Experiments}
\label{sec:experiments}
\begingroup\sloppy
\input{sections/experiments_uai}
\endgroup

\section{Conclusion}
\label{sec:conclusion}
\input{sections/conclusion_uai}

\begin{contributions}
    Contributions will be detailed after the review period.
\end{contributions}

\begin{acknowledgements}
    We thank the anonymous reviewers for their constructive feedback.
\end{acknowledgements}

\begingroup
\sloppy
\setlength{\emergencystretch}{4em}
\raggedright
\bibliography{references}
\endgroup

\newpage

\onecolumn

\title{Why Relation-Agnostic Uncertainty Fails: Decomposing OOD Detection in Knowledge Graphs\\(Supplementary Material)}
\maketitle

\appendix

\section{Theoretical Details}
\label{app:theory}

\subsection{Theorem Proofs}
\label{app:proof}

We state the full assumptions and prove both theorems.

\paragraph{Assumptions.}
\begin{enumerate}
    \item[\textbf{(A1)}] \textbf{Variance-frequency monotonicity}: $\freq(e_1) > \freq(e_2) \Rightarrow \sigma^2_{e_1} < \sigma^2_{e_2}$.
    \item[\textbf{(A2)}] \textbf{ID coverage} (definitional; follows from Definition~\ref{def:ood}): For all $(h,r,t) \in \mathcal{D}_{\text{ID}}$: $c(h,r) = c(t,r) = 1$.
    \item[\textbf{(A3)}] \textbf{Frequency overlap}: For any $(h,r,t) \in \mathcal{D}_{\text{novel}}$, there exists $(h',r',t') \in \mathcal{D}_{\text{ID}}$ with $|\freq(h) - \freq(h')| \leq \epsilon_h$ and $|\freq(t) - \freq(t')| \leq \epsilon_t$.
    \item[\textbf{(A4)}] \textbf{Bounded semantic gap}: $\Delta = \max_{x \in \mathcal{D}_{\text{novel}}, x' \in \mathcal{D}_{\text{ID}}} [\tilde{U}_{\text{sem}}(x') - \tilde{U}_{\text{sem}}(x)] < 1$.
    \item[\textbf{(A5)}] \textbf{Non-degenerate coverage}: $0 < \rho = P(U_{\text{str}}(x) = 0 | x \in \mathcal{D}_{\text{emerge}}) < 1$.
    \item[\textbf{(A6)}] \textbf{Semantic separation}: $\tilde{U}_{\text{sem}}(x) > \tilde{U}_{\text{sem}}(x')$ for $x \in \mathcal{D}_{\text{emerge}}, x' \in \mathcal{D}_{\text{ID}}$.
\end{enumerate}

\subsection{Proof of Theorem~\ref{thm:impossibility} (Impossibility Result)}

\textbf{Theorem statement.} Any uncertainty estimator $U(h,r,t) = f(\sigma^2_h, \sigma^2_t)$ where $\sigma^2_e$ depends only on entity $e$ achieves AUROC $\leq 1/2 + O(\epsilon)$ on novel contexts under assumptions A1--A3.

\textbf{Proof.} Consider the OOD detection task distinguishing $\mathcal{D}_{\text{ID}}$ from $\mathcal{D}_{\text{novel}}$. By definition, novel contexts have $\min(\freq(h), \freq(t)) > \tau$ (high frequency entities) but $c(h,r) = 0$ or $c(t,r) = 0$ (unobserved entity-relation pairs).

\textbf{Step 1: Variance depends only on frequency.} By A1, the learned variance $\sigma^2_e$ is a monotonically decreasing function of $\freq(e)$. In practice, this emerges from Bayesian posterior updates: entities with more observations have tighter (lower variance) posteriors. Therefore, $\sigma^2_e = g(\freq(e))$ for some decreasing function $g$.

\textbf{Step 2: Novel contexts have frequency-matched ID counterparts.} By A3, for any novel-context triple $(h,r,t) \in \mathcal{D}_{\text{novel}}$, there exists an ID triple $(h',r',t') \in \mathcal{D}_{\text{ID}}$ such that $|\freq(h) - \freq(h')| \leq \epsilon_h$ and $|\freq(t) - \freq(t')| \leq \epsilon_t$ for small $\epsilon_h, \epsilon_t$.

\textbf{Step 3: Uncertainty scores are indistinguishable.}
\begin{align}
U(h,r,t) &= f(\sigma^2_h, \sigma^2_t) = f(g(\freq(h)), g(\freq(t))) \\
&\approx f(g(\freq(h')), g(\freq(t'))) = f(\sigma^2_{h'}, \sigma^2_{t'}) = U(h',r',t')
\end{align}
where the approximation error is $O(\epsilon)$ depending on the smoothness of $f$ and $g$.

\textbf{Step 4: AUROC bound.} Since $U(h,r,t)$ for novel contexts has approximately the same distribution as $U(h',r',t')$ for ID triples, the two classes are not separable:
\begin{equation}
\text{AUROC} = P(U^{\text{OOD}} > U^{\text{ID}}) \approx P(U^{\text{ID}} > U^{\text{ID}}) = \frac{1}{2}
\end{equation}
The deviation from exactly 0.5 is $O(\epsilon)$, controlled by the tightness of frequency matching in A3.

\textbf{Implication.} This impossibility result is structural: \emph{any} function $f$ of entity-level statistics fails. It applies to averages ($f = \text{mean}$), maxima ($f = \max$), learned combinations, etc. The only escape is to make $\sigma^2$ depend on the relation $r$, breaking the relation-agnostic assumption. $\square$

\paragraph{Instantiating $O(\epsilon)$.}
We bound $\epsilon$ using the Spearman rank correlation $\rho_s$ between entity frequency and learned variance, which quantifies the tightness of the variance-frequency coupling (A1). Under the Mann-Whitney formulation, $|\text{AUROC} - 0.5| \leq C \sqrt{1 - \rho_s^2}$ where $C \leq 0.5$. On our datasets: WN18RR $\rho_s {=} {-}0.81$ ($\epsilon \leq 0.29$), FB15k-237 $\rho_s {=} {-}0.88$ ($\epsilon \leq 0.24$), YAGO $\rho_s {=} {-}0.68$ ($\epsilon \leq 0.37$). Observed novel-context AUROC: 0.40, 0.49, 0.40 (WN18RR, FB15k-237, YAGO)---all within these bounds. Values near or below 0.5 are consistent with near-random detection on novel contexts, confirming the impossibility bound.

\subsection{Proof of Theorem~\ref{thm:complementarity} (Complementarity)}

\paragraph{Proof of Part (i).}
By (A3), novel-context entities have frequency-matched ID counterparts. By (A1), matching frequencies imply matching variances. Thus $U_{\text{sem}}$ has identical distributions on novel contexts and ID, giving AUROC $= 0.5 + O(\epsilon)$.

\paragraph{Proof of Part (ii).}
By (A2), ID has $U_{\text{str}} = 0$. By (A5), fraction $\rho$ of emerging entities also have $U_{\text{str}} = 0$. Since $U_{\text{str}}$ is discrete, ties between OOD ($U_{\text{str}}=0$) and ID ($U_{\text{str}}=0$) contribute $\tfrac{1}{2}$ per pair: AUROC $= (1 - \rho) + \tfrac{1}{2}\rho = 1 - \tfrac{\rho}{2}$, which is $< 1$ when $\rho > 0$.

\paragraph{Proof of Remark~\ref{rem:novel_perfect}.}
Novel contexts have $c(h,r) = 0$ or $c(t,r) = 0$ by Definition~\ref{def:ood}, so $U_{\text{str}} \geq 1$. ID has $U_{\text{str}} = 0$ by (A2). Since all ID scores equal 0 and all novel-context scores are $\geq 1$, $\text{AUROC} = P(U_{\text{str}}^{\text{OOD}} > U_{\text{str}}^{\text{ID}}) = 1$. \emph{Note:} this is definitional---the OOD label and the detector use the same feature $c(e,r)$. The substantive claim is Theorem~\ref{thm:impossibility}: entity-level methods cannot detect this category, making explicit coverage tracking necessary.

\paragraph{Proof of Part (iii).}
AUROC equals the Mann-Whitney U-statistic: $\text{AUROC}(U) = P(U_{\text{OOD}} > U_{\text{ID}})$. By the law of total probability over OOD categories:
\begin{align}
P(U_{\text{OOD}} > U_{\text{ID}}) &= \pi_e \cdot P(U_{\text{emerge}} > U_{\text{ID}}) + \pi_n \cdot P(U_{\text{novel}} > U_{\text{ID}}) \nonumber \\
&= \pi_e \cdot A_{\text{emerge}}(U) + \pi_n \cdot A_{\text{novel}}(U) + \delta_{\text{tie}}.
\end{align}
This is exact under our fixed tie policy; in continuous-score experiments the tie term is negligible ($\delta_{\text{tie}}\approx 0$). For the synergy formula: since $A_{\text{novel}}(U_{\text{str}}) = 1$ (Remark~\ref{rem:novel_perfect}) and $A_{\text{novel}}(U_{\text{comb}}) = 1$ for $\alpha < 1/(1{+}\Delta)$ (the structural term still separates novel contexts when semantic contamination is bounded), and both tie terms are computed with the same convention, the novel-context terms cancel:
$\text{AUROC}(U_{\text{comb}}) - \text{AUROC}(U_{\text{str}}) = \pi_e \cdot (A_{\text{emerge}}^{\text{comb}} - A_{\text{emerge}}^{\text{str}})$.
This is positive whenever $U_{\text{comb}}$ separates emerging entities better than $U_{\text{str}}$ alone (verified empirically: +8--12pp on all datasets, Table~\ref{tab:complementarity}).

\paragraph{Robustness to Assumption Violations.}
Table~\ref{tab:assumptions} quantifies violations (all values from the reparameterization-fixed model): (A1) Spearman $\rho$ ranges from $-0.68$ to $-0.88$; (A4) $\Delta$ exceeds 1.0 on all three datasets (FB15k-237: 1.10, WN18RR: 1.00, YAGO: 1.36). Despite these violations, qualitative predictions hold: semantic AUROC on novel contexts is 0.40--0.49 (predicted $\lesssim 0.5$), structural is 1.00 (definitional), and the combination improves over entity-level baselines across all datasets. The near-chance novel-context AUROC arises because properly trained variances remove accidental frequency correlation, making novel-context entities' variances indistinguishable from ID entities of similar frequency. Despite A4 being violated, the qualitative predictions align with observations; we conjecture the bound can be relaxed but leave this to future work.

\subsection{Assumption Verification}
\label{app:assumptions}

\begin{table}[ht]
\centering
\caption{Empirical verification of theorem assumptions: variance-frequency monotonicity (A1), bounded semantic gap (A4), non-degenerate coverage (A5), and semantic separation (A6). A2 (ID coverage) is definitional and included for completeness.}
\label{tab:assumptions}
\small
\begin{tabular}{llccc}
\toprule
 & & FB15k-237 & WN18RR & YAGO \\
\midrule
(A1) & Spearman $\rho_s(\text{freq}, \sigma^2)$ & $-$0.88 & $-$0.81 & $-$0.68 \\
(A2) & ID coverage rate & 1.00 & 1.00 & 1.00 \\
(A4) & $\Delta$ (req: $<$1) & 1.10 & 1.00 & 1.36 \\
(A5) & $\rho$ (frac.\ covered emerging) & 0.43 & 0.34 & 0.66 \\
(A6) & Sem. AUROC on emerging & 0.81 & 0.71 & 0.69 \\
\midrule
& Sem. on novel (pred: 0.5) & 0.49 & 0.40 & 0.40 \\
& Str. on novel (pred: 1.0) & 1.00 & 1.00 & 1.00 \\
\bottomrule
\end{tabular}
\end{table}

Assumptions (A1)--(A3) hold across datasets (with YAGO showing weaker A1 monotonicity at $\rho{=}{-}0.68$). (A4) is violated or borderline on all three datasets ($\Delta \geq 1.0$; FB15k-237: 1.10, WN18RR: 1.00 (boundary), YAGO: 1.36), meaning the sufficient condition $\Delta < 1$ for perfect novel-context separation by $U_{\text{comb}}$ does not hold. Despite this, the qualitative predictions---semantic fails on novel contexts, structural fails on some emerging entities, and the combination helps---remain valid across all datasets. The assumptions are idealized sufficient conditions; the theorems provide directional insights whose predictions we validate empirically (Table~\ref{tab:temporal_ood}).

\subsubsection{Empirical Verification of Assumption A3}
\label{app:assumption_verification}

We verify A3 by checking whether each novel-context test triple has a frequency-matched ID counterpart within tolerance $\epsilon$.
On FB15k-237, 100\% of novel-context triples match at every tested $\epsilon \in \{1, 5, 10, 20, 50, 100\}$.
Novel-context entities have mean frequency 35.1 (median 28) vs ID mean 44.7 (median 37), confirming they are individually well-observed but appear in unseen entity-relation combinations.
On WN18RR and YAGO3-10, 98.3\% and 99.7\% match at $\epsilon{=}10$, respectively.
This near-perfect matching validates Theorem~\ref{thm:impossibility}: since novel-context frequencies are indistinguishable from ID, any $U(h,r,t) = f(\sigma^2_h, \sigma^2_t)$ achieves AUROC $\approx 0.5$ on this category.

\section{Additional Experimental Results}
\label{app:experiments}

\subsection{Alpha Sensitivity Analysis}
\label{app:alpha_sensitivity}

\paragraph{Why alpha cannot be learned from the link prediction objective.}
In principle, $\alpha$ could be treated as a learnable parameter. However, the link prediction loss provides \emph{zero gradient} with respect to $\alpha$: all training triples have $U_{\text{str}} = 0$ by construction (both entity-relation pairs are observed in training), so $\partial \mathcal{L}_{\text{BCE}} / \partial \alpha = 0$. The uncertainty margin loss $\mathcal{L}_{\text{margin}}$ does involve OOD triples (random corruptions during training), but random entity corruptions rarely produce zero-coverage triples, providing insufficient signal to learn $\alpha$. In preliminary experiments, the logit-parameterized $\alpha$ remained at its initialization (0.50) across all datasets and seeds, confirming the flat loss landscape.

\paragraph{Post-hoc sensitivity.}
Since $\alpha$ only affects the uncertainty computation (not model training), we evaluate sensitivity by training CAGP once per seed and sweeping $\alpha \in \{0.0, 0.05, 0.1, \ldots, 1.0\}$ at evaluation time. This is computationally free.

Representative results (3-seed mean): on WN18RR, $\alpha{=}0$ yields 0.859 (coverage only), $\alpha{=}0.5$ yields 0.924, and $\alpha{=}1$ yields 0.626 (semantic only). On FB15k-237: 0.935, 0.967, and 0.418. On YAGO3-10: 0.840, 0.899, and 0.440 (Figure~\ref{fig:alpha_sensitivity}). Performance is robust across $\alpha \in [0.05, 0.95]$, with degradation only at the two extremes. The plateau is remarkably flat: on FB15k-237, all $\alpha$ values from 0.05 to 0.90 achieve 0.968$\pm$0.000. This reflects the complementary nature of the signals---the combination is insensitive to the precise mixing weight because the two signals operate on non-overlapping OOD categories.

On ICEWS14, coverage dominates ($>$96\% of temporal OOD has $U_{\text{str}} > 0$), so any $\alpha < 0.9$ achieves $>$0.99 AUROC.

\begin{figure}[ht]
\centering
\includegraphics[width=0.7\linewidth]{figures/fig3_alpha_sensitivity.pdf}
\caption{Alpha sensitivity: AUROC vs.\ mixing weight $\alpha$ on WN18RR, FB15k-237, and YAGO3-10 (3-seed mean $\pm$ std). Performance is robust across $\alpha \in [0.05, 0.95]$, degrading only at the two extremes ($\alpha{=}0$: coverage only; $\alpha{=}1$: semantic only). The broad plateau supports using a fixed $\alpha{=}0.5$ without tuning.}
\label{fig:alpha_sensitivity}
\end{figure}

\paragraph{Recommendation.}
We fix $\alpha{=}0.5$ as the default (Figure~\ref{fig:alpha_sensitivity}). No validation set tuning is required, making CAGP a zero-hyperparameter extension over the base embedding model.

\subsection{Theorem~\ref{thm:complementarity}(iii) Validation: Mixture Decomposition}
\label{app:mixture}

We validate the mixture AUROC decomposition (Eq.~\ref{eq:mixture_auroc}) by comparing predicted and observed AUROC at varying emerging-to-novel ratios. No retraining is performed; we resample from per-triple uncertainties and verify that $\text{AUROC}(U) = \pi_e \cdot A_{\text{emerge}}(U) + (1{-}\pi_e) \cdot A_{\text{novel}}(U) + \delta_{\text{tie}}$ holds to within 0.01 on all datasets.

\begin{table}[ht]
\centering
\caption{Oracle combination gain over best single signal at varying emerging/(emerging+novel) ratios. The combination outperforms at all mixed proportions, validating Theorem~\ref{thm:complementarity}(iii).}
\label{tab:mixture}
\small
\begin{tabular}{lccc|ccc}
\toprule
& \multicolumn{3}{c}{WN18RR} & \multicolumn{3}{c}{FB15k-237} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-7}
Frac. & $U_{\text{sem}}$ & $U_{\text{str}}$ & Oracle & $U_{\text{sem}}$ & $U_{\text{str}}$ & Oracle \\
\midrule
0.0 & .40 & \textbf{1.00} & 1.00 & .49 & \textbf{1.00} & 1.00 \\
0.3 & .50 & .95 & \textbf{.98} & .59 & .94 & \textbf{.98} \\
0.5 & .56 & .92 & \textbf{.97} & .65 & .89 & \textbf{.97} \\
0.7 & .62 & .88 & \textbf{.95} & .72 & .85 & \textbf{.96} \\
1.0 & \textbf{.71} & .83 & \textbf{.93} & \textbf{.82} & .79 & \textbf{.94} \\
\bottomrule
\end{tabular}
\end{table}

At 100\% novel contexts (frac=0.0), $U_{\text{str}}$ dominates; at 100\% emerging (frac=1.0), $U_{\text{sem}}$ dominates. At mixed proportions, the oracle combination consistently outperforms the best single signal by 0.01--0.14 AUROC, confirming that the two signals are genuinely complementary. The oracle $\alpha^*$ is small (0.05--0.07), reflecting the high discriminative power of the structural signal.

\paragraph{Numerical verification of Eq.~\ref{eq:synergy}.}
We compute per-category AUROC from per-triple uncertainties and verify the synergy formula directly. On WN18RR ($\pi_e{=}0.83$): predicted synergy $= 0.83 \times (0.91 - 0.83) = 0.066$, observed $\text{AUROC}(\text{CAGP}) - \text{AUROC}(U_{\text{str}}) = 0.923 - 0.859 = 0.064$. On FB15k-237 ($\pi_e{=}0.30$): predicted $= 0.30 \times (0.89 - 0.78) = 0.033$, observed $= 0.967 - 0.935 = 0.032$. On YAGO3-10 ($\pi_e{=}0.48$): predicted $= 0.48 \times (0.79 - 0.67) = 0.058$, observed $= 0.899 - 0.840 = 0.059$. All predictions match to within 0.002, confirming that the tie-aware decomposition holds in practice and synergy is concentrated in the emerging-entity category.

\subsection{Binary vs Continuous Coverage}
\label{app:coverage_ablation}

\begin{table}[ht]
\centering
\caption{Binary vs continuous coverage on novel-context OOD (FB15k-237). Binary achieves AUROC = 1.0 (by construction; see Remark~\ref{rem:circularity}) while continuous variants fail (AUROC = 0.56--0.59).}
\label{tab:coverage_ablation}
\begin{tabular}{lccc}
\toprule
Coverage Mode & AUROC & AUPR & Separation \\
\midrule
Binary & \textbf{1.0000} & \textbf{1.0000} & 0.360 \\
Log-scaled & 0.5888 & 0.6119 & 0.058 \\
TF-IDF & 0.5606 & 0.5733 & 0.046 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Explanation.}
Binary coverage achieves perfect OOD detection because novel contexts are characterized by \emph{zero} coverage---entity-relation pairs never observed during training. Binary coverage provides clean separation: ID triples have both entities observed with the query relation (uncertainty $= 0$), while OOD triples have at least one entity never observed with that relation (uncertainty $> 0$).

Continuous coverage introduces frequency-based variations among observed pairs. An entity seen once with a relation gets low frequency weight, while an entity seen 100 times gets high frequency weight. This creates overlapping uncertainty distributions between ID (which can have low-frequency observations) and OOD (which may include entities observed frequently with other relations). The result is near-random performance (AUROC $\approx 0.56$--$0.59$).

This validates that the discrete presence/absence signal is fundamental to structural uncertainty, not the continuous frequency information.

\paragraph{Why perfect detection on novel contexts?}
Binary coverage achieves AUROC $= 1.0$ on novel contexts by construction (Remark~\ref{rem:circularity}): the novel-context category is defined by $c(e,r) = 0$, and the detector checks the same feature.
The practical value depends on the fraction of test triples that fall into this category.
On FB15k-237, novel contexts constitute $\sim$25\% of the test set (Table~\ref{tab:complementarity}), making coverage-based detection practically important despite being definitionally guaranteed.
On datasets with more emerging entities (e.g., YAGO3-10), the structural signal alone is less effective, validating the need for the combined approach.

\subsection{Calibration Analysis}
\label{app:calibration}

\begin{table}[ht]
\centering
\caption{Calibration metrics on FB15k-237. ECE = Expected Calibration Error (lower is better).}
\begin{tabular}{lcc}
\toprule
Method & ECE $\downarrow$ & Brier $\downarrow$ \\
\midrule
UKGE & 0.312 & 0.298 \\
Energy & 0.287 & 0.271 \\
Deep Ensemble & 0.198 & 0.203 \\
SNGP & 0.183 & 0.195 \\
$U_{\text{sem}}$ & 0.156 & 0.178 \\
$U_{\text{str}}$ & 0.142 & 0.165 \\
CAGP & \textbf{0.089} & \textbf{0.112} \\
\bottomrule
\end{tabular}
\end{table}

CAGP achieves 37--47\% ECE reduction over single signals. The decomposition produces well-calibrated uncertainties because it separates prediction confidence from structural observation patterns.

\subsection{Selective Prediction}
\label{app:selective}

We present an \emph{oracle analysis} of selective prediction (abstaining on high-uncertainty queries) on FB15k-237 link prediction, designed to illustrate the upper bound on error reduction achievable by each uncertainty signal. The setup follows the standard selective prediction framework~\citep{geifman2017selective}: given a link prediction model that ranks candidate entities, we use each uncertainty method to rank test triples by predicted uncertainty and abstain from the top $k$\% (highest uncertainty). ``Accuracy'' is Hits@1 on the remaining $(100{-}k)$\% of queries, computed using the base DistMult model's entity rankings. We report results at 85\% coverage ($k{=}15$).

\paragraph{Oracle caveat.}
The abstention threshold is selected on the test set; this is \emph{not} a deployable protocol but an oracle evaluation that quantifies the potential of each uncertainty signal for selective prediction. In a deployment setting, a held-out calibration set or conformal prediction framework~\citep{zhu2025certainty} would be required to set the threshold while guaranteeing a target coverage rate.

\paragraph{Methodology details.}
For each test triple $(h,r,?)$, the base model ranks all candidate tail entities by score $s(h,r,t')$. The ``correct'' prediction is Hits@1: whether the ground-truth $t$ is ranked first. Each uncertainty method produces $U(h,r,t)$ for the ground-truth triple; we abstain from the 15\% of triples with highest $U$. All methods share the same base model predictions---only the abstention ranking differs.

\begin{table}[ht]
\centering
\caption{Selective prediction on FB15k-237. Systems answer 85\% of queries. Accuracy = Hits@1 on non-abstained triples. Error reduction relative to no-abstention baseline.}
\begin{tabular}{lcc}
\toprule
Method & Acc@85\% & Error Red. \\
\midrule
No abstention & 62.3\% & --- \\
UKGE & 68.1\% & 15.4\% \\
Deep Ensemble & 69.4\% & 18.8\% \\
$U_{\text{sem}}$ & 71.2\% & 23.6\% \\
$U_{\text{str}}$ & 73.5\% & 29.7\% \\
CAGP & \textbf{78.4\%} & \textbf{42.7\%} \\
\bottomrule
\end{tabular}
\end{table}

Abstaining on 15\% highest-uncertainty queries reduces errors by 43\% (78.4\% accuracy vs 62.3\% baseline). CAGP's advantage over single signals (23.6\% for $U_{\text{sem}}$, 29.7\% for $U_{\text{str}}$) arises because the abstained set captures both poorly-constrained entities (semantic) and novel relational contexts (structural). The combination removes a more diverse set of likely-incorrect predictions than either signal alone.

\subsection{Architecture Generalization}
\label{app:architectures}

\begin{table}[ht]
\centering
\caption{Generalization across architectures on FB15k-237 (standard OOD, random corruptions). CAGP achieves consistent performance (0.959--0.963 AUROC) across DistMult, TransE, and ComplEx.}
\begin{tabular}{lccc}
\toprule
Base Model & $U_{\text{sem}}$ & $U_{\text{str}}$ & CAGP \\
\midrule
DistMult & 0.753 & 0.821 & 0.959 \\
TransE & 0.775 & 0.822 & 0.963 \\
ComplEx & 0.755 & 0.821 & 0.960 \\
\bottomrule
\end{tabular}
\end{table}

The decomposition is architecture-agnostic, achieving $\sim$0.96 AUROC across DistMult, TransE, and ComplEx.

\subsection{Relation-Conditioned Scoring Does Not Help OOD Detection}
\label{app:bilinear}

To test whether relation-conditioned scoring improves entity-level OOD detection, we replace DistMult's element-wise product with a bilinear scorer $h^\top W_r t$, where $W_r = \sum_{b=1}^{B} c_{rb} \mathbf{B}_b$ uses basis decomposition ($B{=}10$ bases). Entity embeddings retain the same variational structure (mean + logvar with reparameterization sampling). Uncertainty remains entity-level: $U(h,r,t) = (\exp(\text{logvar}_h) + \exp(\text{logvar}_t))/2$---identical to $U_{\text{sem}}$, with $r$ unused.

\begin{table}[ht]
\centering
\caption{Bilinear scoring vs.\ entity-level baselines on temporal OOD (3-seed mean). Per-relation bilinear scoring does not change entity-level uncertainty. Neither $U_{\text{sem}}$ nor Bilinear achieves novel-context AUROC above 0.55, consistent with Theorem~\ref{thm:impossibility}. Adding coverage (Bilinear+CAGP) recovers to match standard CAGP.}
\label{tab:bilinear}

\scriptsize
\setlength{\tabcolsep}{2pt}
\begin{tabular}{l ccc ccc ccc}
\toprule
 & \multicolumn{3}{c}{WN18RR} & \multicolumn{3}{c}{FB15k-237} & \multicolumn{3}{c}{YAGO} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}\cmidrule(lr){8-10}
Method & Em. & Nov. & All & Em. & Nov. & All & Em. & Nov. & All \\
\midrule
$U_{\text{sem}}$ & .66 & .41 & .62 & .62 & .34 & .42 & .80 & .42 & .60 \\
Bilinear & .71 & .37 & .65 & .69 & .40 & .49 & .82 & .43 & .62 \\
\midrule
CAGP & .90 & 1.0 & .92 & .89 & 1.0 & .97 & .79 & 1.0 & .90 \\
Bilinear+CAGP & .93 & 1.0 & .94 & .93 & 1.0 & .98 & .85 & 1.0 & .93 \\
\bottomrule
\end{tabular}
\end{table}

Neither $U_{\text{sem}}$ nor the bilinear scorer achieves novel-context AUROC above 0.43 on any dataset (3-seed mean), consistent with Theorem~\ref{thm:impossibility}: relation-conditioned \emph{scoring} does not produce relation-aware \emph{uncertainty}. Adding coverage (Bilinear+CAGP) recovers to within 0.03 of standard CAGP on all datasets, confirming that the coverage signal---not the scoring function---drives novel-context detection.

\subsection{Uncertainty Distribution Visualization}
\label{app:distributions}

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{figures/fig2_complementarity.pdf}
\caption{Uncertainty distributions on FB15k-237, illustrating why neither signal alone suffices. \textbf{Top row} ($U_{\text{sem}}$): semantic uncertainty overlaps heavily between ID and emerging (AUC=0.62) and is \emph{below random} on novel contexts (AUC=0.34)---high-frequency entities have low variance regardless of relational experience. \textbf{Bottom row} ($U_{\text{str}}$): structural uncertainty perfectly separates novel contexts (AUC=1.00) but ${\sim}44\%$ of emerging entities have $U_{\text{str}}{=}0$ (covered for the query relation), making them indistinguishable from ID (AUC=0.78). CAGP combines both signals to achieve 0.97 overall.}
\label{fig:complementarity}
\end{figure}

\subsection{Additional Discussion}
\label{app:discussion}

\paragraph{Dataset-specific signal composition.}
Table~\ref{tab:temporal_ood} reveals the optimal signal composition varies by dataset. FB15k-237 is dominated by novel contexts (25.4\% of test set), where structural uncertainty excels. On YAGO3-10, structural uncertainty remains the stronger single-signal contributor ($U_{\text{str}}$ 0.84), while semantic uncertainty alone is moderate (0.54)---a similar pattern to WN18RR, where few relations limit GP expressiveness. CAGP improves over $U_{\text{str}}$ on all datasets by contributing semantic information on emerging entities: +8pp on WN18RR, +11pp on FB15k-237, +12pp on YAGO. The improvement is largest where single-signal emerging detection is weakest, consistent with the complementarity predicted by Theorem~\ref{thm:complementarity}. Note: without reparameterization sampling, entity variances collapse under KL regularization (see error analysis in \S\ref{sec:experiments}), reducing CAGP to a coverage-only detector. CAGP achieves 0.90--0.97 AUROC across static benchmarks and 0.993/0.987 on ICEWS14/18.

\paragraph{Why doesn't the embedding model discover coverage?}
A natural question: if coverage is so effective, why don't learned embeddings discover it automatically? The impossibility theorem (Theorem~\ref{thm:impossibility}) provides the answer: standard link prediction objectives incentivize predictive accuracy, not relation-specific uncertainty differentiation. The model learns entity-level statistics (frequency, centrality) but has no signal to distinguish whether an entity's uncertainty should depend on the query relation. Explicit coverage tracking provides this missing inductive bias.

\subsection{AUPR Results}
\label{app:aupr}

\begin{table}[ht]
\centering
\caption{Temporal OOD detection AUPR (mean over 3 seeds).}
\small
\begin{tabular}{lcccc}
\toprule
Method & WN18RR & FB15k-237 & YAGO & ICEWS14 \\
\midrule
UKGE & 0.64 & 0.45 & 0.42 & 0.40 \\
Energy & 0.72 & 0.56 & 0.46 & 0.61 \\
MC Dropout & 0.67 & 0.52 & 0.19 & 0.57 \\
Deep Ensemble & 0.71 & 0.51 & 0.40 & 0.66 \\
SNGP & 0.79 & 0.46 & 0.48 & 0.63 \\
\midrule
$U_{\text{sem}}$ & 0.79 & 0.77 & 0.42 & 0.82 \\
$U_{\text{str}}$ & 0.90 & 0.92 & 0.76 & 0.99 \\
CAGP & \textbf{0.90} & \textbf{0.92} & \textbf{0.76} & \textbf{0.99} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Why CAGP = $U_{\text{str}}$ on AUPR.} AUPR rewards high-precision retrieval. On all static benchmarks, novel-context triples ($c(h,r)=0$ or $c(t,r)=0$) receive coverage uncertainty $\geq 1$ and are ranked above all emerging-entity triples in both $U_{\text{str}}$ and CAGP. Since novel contexts constitute the most precisely-detected positives, the PR curve's high-precision region is identical for both methods. CAGP's improvement over $U_{\text{str}}$ is concentrated in the lower-precision emerging-entity regime (recovered by the GP signal), which contributes minimally to AUPR. This confirms that AUPR and AUROC measure complementary aspects: AUROC reflects the full emerging-entity gain (+8--12pp), while AUPR is dominated by coverage-driven novel-context detection.

\subsection{Error Analysis}
\label{app:error_analysis}

We analyzed CAGP's failure modes on FB15k-237 standard OOD detection (random tail corruption, 20,000 ID + 20,000 OOD samples).

\begin{table}[ht]
\centering
\caption{Error analysis on FB15k-237 standard OOD. FP = false positive (ID flagged as OOD), FN = false negative (OOD missed).}
\label{tab:error_analysis}
\small
\begin{tabular}{lrr}
\toprule
Metric & Value & Rate \\
\midrule
\multicolumn{3}{l}{\textit{Overall Performance}} \\
AUROC & 0.971 & --- \\
Accuracy & 91.3\% & --- \\
Precision & 0.913 & --- \\
Recall & 0.913 & --- \\
F1 Score & 0.913 & --- \\
\midrule
\multicolumn{3}{l}{\textit{Confusion Matrix}} \\
True Negatives & 18,267 & --- \\
False Positives & 1,733 & 8.7\% \\
False Negatives & 1,733 & 8.7\% \\
True Positives & 18,267 & --- \\
\midrule
\multicolumn{3}{l}{\textit{False Positive Characteristics}} \\
Avg tail degree & 183 & (ID: 583) \\
Avg relation freq & 2,384 & (ID: 5,213) \\
\midrule
\multicolumn{3}{l}{\textit{False Negative Characteristics}} \\
Avg tail degree & 183 & (OOD: 37) \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Key findings.}
Table~\ref{tab:error_analysis} shows that \textbf{false positives} (8.7\%) occur primarily on ID triples with low-degree tail entities and rare relations.
These triples have elevated uncertainty despite being in-distribution, reflecting the model's conservative behavior on under-represented patterns.
FP tail entities have mean degree 183 compared to 583 for correctly classified ID triples; FP relations have mean frequency 2,384 vs 5,213.

\textbf{False negatives} (8.7\%) occur when random corruptions coincidentally create higher-degree entities (mean degree 183) compared to typical corruptions (mean degree 37).
These corrupted tails are better-represented in the training data, leading to lower uncertainty.

The balanced error rates (8.7\% FP = FN) and high AUROC (0.971) demonstrate effective discrimination.
Both error types relate to entity degree and relation frequency, validating that semantic uncertainty (which captures these statistics) contributes meaningfully alongside structural uncertainty.

\section{Implementation Details}
\label{app:implementation}

\subsection{OOD Split Methodology}
\label{app:ood_splits}

\paragraph{Simulated temporal OOD.}
We create simulated temporal OOD splits by partitioning test sets based on entity frequency and entity-relation coverage, capturing the key temporal challenge: detecting when familiar entities appear in unfamiliar relational contexts.

\paragraph{Categorization protocol.}
For each test triple $(h,r,t)$:
\begin{itemize}
\item \textbf{Emerging entities}: $\min(\text{freq}(h), \text{freq}(t)) \leq \tau$ where $\tau$ is the 25th percentile of training entity frequencies. These are rare or newly-appeared entities with few training observations.

\item \textbf{Novel contexts}: $\min(\text{freq}(h), \text{freq}(t)) > \tau$ and ($c(h,r)=0$ or $c(t,r)=0$), where $c(e,r)$ indicates whether entity $e$ was observed with relation $r$ during training. These are well-established entities appearing in previously unobserved relational patterns.

\item \textbf{In-distribution}: Both entities are frequent ($> \tau$) and both have coverage for the query relation.
\end{itemize}

\paragraph{Rationale.}
This protocol captures temporal dynamics: in real temporal KGs, entity frequency correlates with ``age'' (older entities have more observations), and novel contexts arise when established entities form new relationships over time. The frequency threshold $\tau$ at the 25th percentile ensures the ``emerging'' category captures genuinely rare entities while ``novel contexts'' includes only well-observed entities.

\paragraph{FB15k-237 split statistics.}
Our temporal OOD split yields:
\begin{itemize}
\item Emerging entities: 2,223 triples (10.9\%, mean freq=3.2, median=2)
\item Novel contexts: 5,193 triples (25.4\%, mean freq=35.1, median=28)
\item In-distribution: 13,050 triples (63.8\%, mean freq=44.7, median=37)
\end{itemize}

The clear frequency separation (emerging: median 2 vs novel contexts: median 28) confirms that these categories represent distinct OOD phenomena.

\paragraph{ICEWS14: ground-truth temporal validation.}
ICEWS14~\citep{garcia2018learning} is a temporal KG containing 7,128 entities, 230 relations, and events from 2014 with ground-truth timestamps. The train/test split is chronological (earlier timestamps for training, later for test), so temporal OOD arises naturally from the evolution of geopolitical events. We apply the \emph{same categorization protocol} to test triples using entity frequencies and coverage computed from the training set. The resulting split---1,264 emerging, 2,982 novel contexts, 8,976 ID---reflects genuine temporal dynamics: 32\% of test triples involve novel entity-relation pairs unseen during training. Crucially, the novel-context category is now defined by \emph{time} (these are entity-relation pairs that first appear in later timestamps), not by coverage---breaking the definitional coupling (Remark~\ref{rem:circularity}) that affects the three static benchmarks.

\subsection{Training Configuration}
\label{app:training_config}

Embedding dimension $d=100$, learning rate $10^{-3}$, averaged over 3 seeds.
WN18RR, FB15k-237, and ICEWS14: batch size 1024, KL weight $\beta_{\text{KL}}=0.001$, 30 epochs.
YAGO3-10: same canonical pipeline (batch size 1024, $\beta_{\text{KL}}=0.001$, 30 epochs).
We report mean$\pm$std over seeds; with $n{=}3$--5 seeds, bootstrap p-values are not reliable (minimum achievable $p{\approx}0.125$ for $n{=}3$) and are not reported.

\paragraph{Scalability Analysis.}
\textbf{Memory complexity.} CAGP's coverage matrix $\mat{C} \in \{0,1\}^{|\mathcal{E}| \times |\mathcal{R}|}$ requires:
\begin{itemize}
\item FB15k-237: $14{,}541 \times 237 = 3.4$M entries $\approx$ 13MB (dense float32)
\item YAGO3-10: $123{,}182 \times 37 = 4.6$M entries $\approx$ 18MB dense
\item Wikidata-scale (90M entities, 1K relations): 360GB dense
\end{itemize}
Sparse storage reduces memory: $<$5\% non-zero on FB15k-237 yields $<$1MB. For extremely large KGs, use relation-specific hash tables storing only observed $(e,r)$ pairs, requiring $O(|\mathcal{T}|)$ memory where $|\mathcal{T}|$ is training triples.

\textbf{Inference complexity.} Computing $U_{\text{str}}(h,r,t)$ requires two hash lookups: $O(1)$ average case. Total overhead: $<$2\% vs forward pass (measured on FB15k-237).

\textbf{Learned variance alternative.} We explored learning relation-conditioned variance $\sigma^2(e,r)$ via multi-layer perceptron, avoiding explicit coverage storage. However, this requires auxiliary OOD objectives and provides marginal benefits over explicit coverage. We found CAGP's interpretability and simplicity preferable.

\paragraph{Threshold $\tau$ sensitivity.}
We set $\tau$ to the 25th percentile of entity frequencies. In preliminary experiments on FB15k-237, varying $\tau$ from the 5th to the 30th percentile produced $<$0.02 AUROC variation for CAGP, confirming robustness to this choice. Higher thresholds ($>$30th percentile) reduce the emerging entity set, slightly hurting detection.

\subsection{Dataset Statistics}
\label{app:datasets}

\begin{table}[ht]
\centering
\caption{Dataset statistics: number of entities, relations, and triples in train/validation/test splits.}
\begin{tabular}{lccccc}
\toprule
Dataset & Entities & Relations & Train & Valid & Test \\
\midrule
WN18RR & 40,943 & 11 & 86,835 & 3,034 & 3,134 \\
FB15k-237 & 14,541 & 237 & 272,115 & 17,535 & 20,466 \\
YAGO3-10 & 123,182 & 37 & 1,079,040 & 5,000 & 5,000 \\
ICEWS14 & 7,128 & 230 & 63,685 & 13,823 & 13,222 \\
ICEWS18 & 23,033 & 256 & 373,018 & 45,995 & 49,545 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{ICEWS14 Leakage Audit}
\label{app:leakage}

We conducted a comprehensive leakage audit on ICEWS14 to verify the integrity of the temporal OOD evaluation.

\paragraph{Temporal split integrity.}
Train timestamps (IDs 0--6264, 262 unique) are strictly earlier than test timestamps (IDs 7536--8736, 51 unique), with zero overlap. The chronological split has no temporal leakage.

\paragraph{Inverse relations.}
53.1\% of test triples (7,021/13,222) have an inverse counterpart $(t,r',h)$ in training (same or different $r'$). This is expected in temporal event KGs where political interactions are inherently reciprocal (e.g., ``A visits B'' implies ``B hosts A''). Relations 7 and 14 form a perfect reciprocal pair; relations 18, 41, 44 are self-reciprocal. Importantly, inverse relations do \emph{not} inflate coverage-based detection: our structural signal tracks entity-\emph{relation} pairs $c(e,r)$, not entity pairs---an entity observed with $r$ may have zero coverage for $r'$.

\paragraph{Exact triple repetition.}
28.2\% of unique test triples repeat a training $(h,r,t)$ at a different timestamp (recurring events). These triples fall into the ID category by definition (both entity-relation pairs are observed), making the ID pool easier to classify---which, if anything, \emph{helps} baselines. Yet baselines score only 0.38--0.80 (Table~\ref{tab:temporal_ood}).

\paragraph{Impact on OOD categories.}
The novel-context category (2,982 triples, 22.6\%) has zero overlap with exact training duplicates---all are genuinely new entity-relation combinations that first appear in later timestamps. Emerging entities (1,264 triples) include 68\% with at least one entity completely absent from training.

\paragraph{Strict split validation.}
To rule out transductive artifacts, we construct a \emph{strict test set} by removing all exact duplicates and inverse overlaps (58.5\% removed: 715 exact-only + 2,555 inverse-only + 4,466 both; remaining 5,486 = 1,223 emerging + 1,886 novel + 2,377 ID). Table~\ref{tab:strict_split} shows that CAGP's AUROC \emph{improves} from 0.993 to 0.995, while Energy drops from 0.59 to 0.50, ruling out train/test memorization artifacts.

\begin{table}[ht]
\centering
\caption{ICEWS14: original vs strict split (3-seed mean$\pm$std). Strict split removes all exact+inverse overlaps (58.5\% of test triples). CAGP improves; Energy drops.}
\label{tab:strict_split}
\small
\begin{tabular}{lcc}
\toprule
Method & Original AUROC & Strict AUROC \\
\midrule
UKGE & .379$\pm$.007 & .391$\pm$.016 \\
Energy & .590$\pm$.009 & .499$\pm$.004 \\
$U_{\text{sem}}$ & .836$\pm$.000 & .823$\pm$.000 \\
$U_{\text{str}}$ & .993$\pm$.000 & .995$\pm$.000 \\
\textbf{CAGP} & \textbf{.993$\pm$.000} & \textbf{.995$\pm$.000} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{ICEWS14 Ablation: Structural Signal Necessity}
\label{app:icews14_ablation}

To confirm that CAGP's high AUROC on ICEWS14 is driven by the specific entity-relation structure rather than statistical artifacts, we evaluate five ablation variants (Table~\ref{tab:icews14_ablation}). All share identical training; only the uncertainty computation differs at evaluation time.

\begin{table}[ht]
\centering
\caption{ICEWS14 ablation (3-seed mean$\pm$std). Removing or corrupting the structural signal degrades AUROC to near-random, confirming that the specific entity-relation coverage structure drives detection.}
\label{tab:icews14_ablation}
\small
\begin{tabular}{lccc}
\toprule
Variant & Overall & Emerging & Novel Ctx \\
\midrule
CAGP (full) & \textbf{.993$\pm$.000} & \textbf{1.00$\pm$.00} & \textbf{1.00$\pm$.00} \\
$\alpha{=}0$ (cov.\ only) & .993$\pm$.000 & .975$\pm$.000 & 1.00$\pm$.00 \\
$\alpha{=}1$ (sem.\ only) & .500$\pm$.000 & 1.00$\pm$.00 & .500$\pm$.00 \\
Shuffled coverage & .551$\pm$.016 & .593$\pm$.118 & .573$\pm$.019 \\
Random coverage & .502$\pm$.004 & .809$\pm$.219 & .502$\pm$.004 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Key findings.}
(1)~Removing structural signal ($\alpha{=}1$, $U_{\text{sem}}$ only) drops overall AUROC from 0.99 to 0.50---semantic variance alone cannot detect temporal OOD.
(2)~Shuffling coverage rows (preserving identical row/column sums and density) drops AUROC to 0.55, proving the model exploits \emph{specific} entity-relation associations, not aggregate statistics.
(3)~Random binary coverage at matched density (1.7\%) yields 0.50, ruling out density-driven performance.
(4)~Coverage alone ($\alpha{=}0$) matches full CAGP at 0.99 overall, indicating structural (coverage) signal is the primary driver on temporal OOD, while semantic uncertainty contributes complementary value on emerging-entity slices (0.975 vs 1.00).

\subsection{ICEWS18 Per-Subset Breakdown}
\label{app:icews18_subsets}

Table~\ref{tab:icews18_subsets} reports ICEWS18 results broken down by OOD category (emerging entities, novel contexts, and overall). These results use the same evaluation pipeline and chronological train/test split as ICEWS14. Split sizes: 2,747 emerging, 8,587 novel contexts, 38,211 ID.

\begin{table}[ht]
\centering
\caption{ICEWS18 per-subset AUROC (3-seed mean). $U_{\text{str}}$ and CAGP are identical across all subsets: coverage fully saturates detection and the semantic signal adds no lift, confirming a ceiling effect on this benchmark.}
\label{tab:icews18_subsets}
\small
\begin{tabular}{lccc}
\toprule
Method & Emerging & Novel Ctx & Overall \\
\midrule
UKGE & .808 & .757 & .776 \\
Energy & .924 & .809 & .865 \\
GPOnly ($U_{\text{sem}}$) & .985 & .828 & .908 \\
\midrule
$U_{\text{str}}$ & .976 & 1.000 & .987 \\
CAGP & .976 & 1.000 & .987 \\
\bottomrule
\multicolumn{4}{l}{\scriptsize Std $< 0.001$ across all 3 seeds for all methods.}
\end{tabular}
\end{table}

\paragraph{Discussion.}
The identical CAGP and $U_{\text{str}}$ values arise because ICEWS18 temporal OOD is dominated by novel-context triples ($>$96\% have $U_{\text{str}} > 0$), leaving the emerging-entity slice (2,747 triples) with near-perfect coverage due to the recurrent event structure of geopolitical KGs. The semantic signal ($U_{\text{sem}}$) achieves 0.985 on this slice alone---but combining with coverage does not improve beyond what coverage already achieves. This is the ceiling effect discussed in the main text: CAGP's decomposition adds analytical clarity but not additional detection power on ICEWS18.

\subsection{Uncertainty Margin Loss Ablation}
\label{app:margin_ablation}

As disclosed in the main text, models with learned variance (GPOnly, CAGP, RelCondVar) receive an uncertainty margin loss during training that encourages higher uncertainty on negative samples. Baselines without variance parameters (UKGE, Energy, CoverageOnly) cannot use this loss because they have no variance parameter to optimize. This section quantifies the effect of the margin loss on GPOnly and CAGP.

\paragraph{Setup.}
We train GPOnly and CAGP on WN18RR with seed 42, setting $w_{\text{unc}}{=}0$ (no margin loss) versus the default $w_{\text{unc}}{=}0.1$. All other hyperparameters are identical.

\begin{table}[ht]
\centering
\caption{Effect of uncertainty margin loss on WN18RR temporal OOD AUROC (seed 42). The margin loss has negligible effect on both GPOnly and CAGP, confirming it does not create a meaningful performance advantage over baselines.}
\label{tab:margin_ablation}
\small
\begin{tabular}{lcccc}
\toprule
Method & $w_{\text{unc}}$ & Emerging & Novel Ctx & Overall \\
\midrule
GPOnly & 0.0 (ablation) & .663 & .405 & .620 \\
GPOnly & 0.1 (default) & .663 & .405 & .621 \\
\midrule
CAGP & 0.0 (ablation) & .912 & 1.000 & .927 \\
CAGP & 0.1 (default) & .911 & 1.000 & .926 \\
\bottomrule
\multicolumn{5}{l}{\scriptsize Differences $< 0.001$; seed 42 only.}
\end{tabular}
\end{table}

The margin loss has negligible effect on temporal OOD AUROC for both models ($\Delta < 0.001$). This holds because: (1)~for CAGP, coverage already provides the dominant structural signal and the margin loss cannot add information beyond what binary coverage conveys; (2)~for GPOnly, frequency-based variance already captures the primary signal for emerging entities. The training signal asymmetry is therefore a structural transparency disclosure, not a driver of the reported performance advantage. Coverage ($U_{\text{str}}$) is deterministic and unaffected by the margin loss, so the disclosed asymmetry does not affect the key comparison between CAGP and $U_{\text{str}}$.

\subsection{Reproducibility: Result Provenance}
\label{app:provenance}

\begin{table}[ht]
\centering
\caption{Mapping of paper tables to experimental sources. All code is included in the supplementary material.}
\small
\begin{tabular}{llll}
\toprule
Table & Dataset & Seeds & Source \\
\midrule
1 & WN18RR (CAGP) & 42, 123, 456 & \texttt{test\_cagp\_fix\_multiseed.py} \\
1 & WN18RR (baselines$^b$) & 42, 123, 456 & \texttt{run\_wn18rr\_missing\_baselines.py} \\
1 & FB15k-237 (CAGP) & 42, 123, 456 & \texttt{test\_cagp\_fix\_multiseed.py} \\
1 & FB15k-237 (baselines$^b$) & 42, 123, 456 & \texttt{run\_wn18rr\_missing\_baselines.py} \\
1 & YAGO3-10$^c$ & 42, 123, 456 & \texttt{test\_cagp\_fix\_multiseed.py} \\
1 & ICEWS14 (ours$^a$) & 42, 123, 456 & \texttt{run\_icews14\_temporal.py} \\
1 & ICEWS14 (baselines$^b$) & 42, 123, 456 & \texttt{run\_icews14\_missing\_baselines.py} \\
1 & ICEWS18 (ours$^a$) & 42, 123, 456 & \texttt{run\_icews14\_temporal.py} \\
1 & ICEWS18 (baselines$^b$) & 42, 123, 456 & \texttt{run\_icews14\_temporal.py} \\
\ref{tab:icews14_ablation} & ICEWS14 & 42, 123, 456 & \texttt{icews14\_ablation.py} \\
\ref{tab:strict_split} & ICEWS14 & 42, 123, 456 & \texttt{icews14\_strict\_split.py} \\
\midrule
2 & FB15k-237 & 42, 123, 456 & \texttt{run\_wn18rr\_temporal.py} \\
2 & WN18RR & 42, 123, 456 & \texttt{run\_wn18rr\_temporal.py} \\
\midrule
3 & All & (same as Table 1) & (same as Table 1) \\
4 & All & (same as Table 1) & (same as Table 1) \\
5 & FB15k-237 & 42 & \texttt{run\_focused\_experiments.py} \\
\ref{tab:bilinear} & All & 42, 123, 456 & \texttt{run\_bilinear\_theorem1.py} \\
Calibration & FB15k-237 & 42 & \texttt{run\_focused\_experiments.py} \\
Selective pred. & FB15k-237 & 42 & \texttt{run\_focused\_experiments.py} \\
\bottomrule
\end{tabular}
\vspace{2pt}

\scriptsize
$^a$UKGE, Energy, $U_{\text{sem}}$, $U_{\text{str}}$, CAGP, RelCondVar. \quad
$^b$MC Dropout, Deep Ensemble, SNGP. \quad
$^c$Reported YAGO methods use 3 seeds; all baseline values in tables are from completed runs.
\end{table}

\subsection{Per-Seed Breakdowns}
\label{app:per_seed}

\begin{table}[ht]
\centering
\caption{Per-seed CAGP temporal OOD AUROC (overall and emerging-entity). Coverage ($U_{\text{str}}$) is deterministic given the data split, so all seeds yield identical values. WN18RR uses 5 seeds; FB15k-237 and YAGO3-10 use 3 seeds.}
\small
\begin{tabular}{llccccc}
\toprule
Dataset & Metric & Seed 42 & Seed 123 & Seed 456 & Seed 789 & Seed 1000 \\
\midrule
WN18RR & Overall & .926 & .917 & .926 & .918 & .928 \\
       & Emerging & .911 & .900 & .911 & .902 & .913 \\
\midrule
FB15k-237 & Overall & .966 & .967 & .967 & --- & --- \\
          & Emerging & .888 & .889 & .890 & --- & --- \\
\midrule
YAGO3-10 & Overall & .894 & .902 & .902 & --- & --- \\
         & Emerging & .779 & .796 & .797 & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

Variance across seeds is small: WN18RR 5-seed std$=$0.005, FB15k-237 std$<$0.001, YAGO std$=$0.004. The variation arises entirely from the learned entity variances (different random initializations), since coverage is deterministic. Novel-context AUROC is 1.000 for all seeds on all datasets (by construction; Remark~\ref{rem:circularity}).

\end{document}
