% Introduction for UAI - condensed

Knowledge graph (KG) embeddings enable efficient reasoning over structured knowledge~\citep{bordes2013translating,trouillon2016complex}.
As KGs evolve---new entities emerge, existing entities appear in novel relationships---robust uncertainty quantification becomes critical.

\paragraph{The Relation-Agnostic Limitation.}
Recent probabilistic methods learn distributions over embeddings~\citep{chen2019embedding,chen2021probabilistic,Chen2021PERM}, where rare entities have higher variance.
But these variances are \emph{relation-agnostic}: entity $e$ receives a single variance $\sigma^2_e$ regardless of relational context.
Consider $e$ appearing in 1000 triples with relations $\{r_1, \ldots, r_{10}\}$ but never with $r_{11}$.
The model assigns low variance (well-observed), yet queries $(?, r_{11}, e)$ involve genuinely uncertain predictions.

This causes systematic failures (Figure~\ref{fig:main_results}): probabilistic methods achieve only 0.38--0.80 AUROC on temporal OOD, while our decomposition-based approach reaches 0.90--0.99. ICEWS14 and ICEWS18 with ground-truth timestamps are the non-circular validations (0.993 and 0.987 AUROC).

\paragraph{Two OOD Types.}
We identify two distinct scenarios:
(1) \textbf{Emerging entities}: New/rare entities with poorly-constrained embeddings---high variance correctly signals uncertainty.
(2) \textbf{Novel contexts}: Well-observed entities in unobserved relations---variance is low (falsely confident), but the entity-relation pair was never trained.

\paragraph{Contributions.}
We diagnose why probabilistic KG embeddings fail at temporal OOD detection and show how to fix them.

\textbf{(1) Impossibility result:} We prove that any relation-agnostic uncertainty estimator---whose output uses only entity-level statistics---is near-random on novel relational contexts (Theorem~\ref{thm:impossibility}). This explains the weak temporal-OOD performance of UKGE, Energy, MC Dropout, Deep Ensembles, and SNGP (0.38--0.80 AUROC).
\textbf{(2) Decomposition with mixture AUROC:} We split KG uncertainty into \emph{semantic} (entity variance) and \emph{structural} (entity--relation co-occurrence) components, with complementary failure modes (Theorem~\ref{thm:complementarity}): semantic uncertainty is effective on emerging entities but weak on novel contexts; structural uncertainty is strong on novel contexts but imperfect on emerging entities. An exact mixture AUROC decomposition predicts overall results within 0.002 on all datasets.
\textbf{(3) Minimal realization validates the framework:} We construct CAGP, which combines semantic and structural signals, to test the decomposition. On ICEWS14/18 temporal splits, CAGP reaches 0.99 AUROC, confirming the framework beyond static artifacts. On WN18RR~\citep{dettmers2018convolutional}, FB15k-237~\citep{toutanova2015observed}, and YAGO3-10~\citep{mahdisoltani2015yago3}, CAGP reaches 0.79--0.91 AUROC on emerging entities (+8--12pp over coverage alone), while overall AUROC (0.90--0.97) matches the mixture formula.
\textbf{(4) Variance collapse diagnosis:} We identify a subtle failure mode in variational KG embeddings: when the scoring function ignores variance parameters, KL regularization collapses all entity variances to a uniform value. Reparameterization sampling in the scoring function prevents this collapse, enabling meaningful semantic uncertainty. This diagnosis applies broadly to probabilistic KG embedding methods.

\begin{figure*}[t]
\centering
\includegraphics[width=0.75\textwidth]{figures/fig1_main_results.pdf}

\caption{Temporal OOD detection on FB15k-237~\citep{toutanova2015observed} (AUROC). Existing probabilistic methods achieve 0.42--0.63; CAGP reaches 0.97. The pattern holds across all evaluated benchmarks (Table~\ref{tab:temporal_ood}).}
\label{fig:main_results}
\end{figure*}
