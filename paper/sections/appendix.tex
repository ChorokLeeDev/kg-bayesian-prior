% Appendix

\section{Proof of Theorem~\ref{thm:coverage_auroc}}
\label{app:proof}

\paragraph{Setup.}
Let $a = P(\text{both covered} | \text{ID})$, $b = P(\text{exactly one covered} | \text{ID})$, $c = 1 - a - b$.
Let $s_r$ be relation sparsity and $q = 1 - s_r$ (probability random entity is covered).

\paragraph{Uncertainty Distributions.}

ID triples:
\begin{align}
    P(U_{\text{ID}} = 0) &= a \\
    P(U_{\text{ID}} = 1) &= b \\
    P(U_{\text{ID}} = 2) &= c
\end{align}

OOD triples (head from ID, random tail):
\begin{align}
    P(U_{\text{OOD}} = 0) &= q \cdot p_h \\
    P(U_{\text{OOD}} = 1) &= q(1-p_h) + (1-q)p_h = q + p_h - 2qp_h \\
    P(U_{\text{OOD}} \geq 1) &\approx s_r \text{ (simplified)}
\end{align}

\paragraph{AUROC Derivation.}

\begin{align}
    \text{AUROC} &= P(U_{\text{ID}} < U_{\text{OOD}}) + \frac{1}{2}P(U_{\text{ID}} = U_{\text{OOD}}) \\
    &= a \cdot s_r + \frac{1}{2}[a(1-s_r) + b \cdot s_r] \\
    &= as_r + \frac{1}{2}a - \frac{1}{2}as_r + \frac{1}{2}bs_r \\
    &= \frac{1}{2}a + \frac{1}{2}as_r + \frac{1}{2}bs_r \\
    &= \frac{1}{2}(a(1+s_r) + s_r \cdot b)
\end{align}

\section{Implementation Details}
\label{app:implementation}

\paragraph{Hyperparameters.}
Embedding dimension $d=100$, batch size 2048, learning rate $10^{-3}$, KL weight $0.01$.
All experiments use 50 epochs and are averaged over 3 seeds (42, 123, 456).

\paragraph{Coverage Computation.}
Coverage matrix $\mat{C} \in \{0,1\}^{|\mathcal{E}| \times |\mathcal{R}|}$ is precomputed from training triples:
\begin{equation}
    C_{e,r} = \mathbb{1}[\exists (h,r,t) \in \mathcal{T} : h=e \lor t=e]
\end{equation}
Storage is sparse: typically $<$5\% of entries are non-zero.

\paragraph{CAGP Implementation.}
\begin{lstlisting}[language=Python]
def cagp_uncertainty(h, r, t, gp_var, coverage, alpha):
    # GP uncertainty (semantic)
    u_gp = (gp_var[h] + gp_var[t]) / 2
    # Coverage uncertainty (structural)
    u_cov = 2.0 - coverage[h, r] - coverage[t, r]
    # Normalize GP to coverage scale
    u_gp_norm = u_gp * u_cov.mean() / u_gp.mean()
    return alpha * u_gp_norm + (1 - alpha) * u_cov
\end{lstlisting}

\section{Extended Results}
\label{app:extended}

Table~\ref{tab:full_results} provides results with standard deviations across 3 random seeds.
Figure~\ref{fig:theorem} visualizes the theorem validation from Section~\ref{sec:experiments}.

\begin{table}[h]
\centering
\caption{Full results with standard deviations (3 seeds).}
\label{tab:full_results}
\begin{tabular}{lccc}
\toprule
Method & WN18RR & FB15k-237 & YAGO3-10 \\
\midrule
Coverage-only & 0.657 $\pm$ 0.002 & 0.821 $\pm$ 0.001 & 0.760 $\pm$ 0.002 \\
GP-only & 0.647 $\pm$ 0.008 & 0.749 $\pm$ 0.005 & 0.824 $\pm$ 0.004 \\
CAGP & 0.871 $\pm$ 0.003 & 0.960 $\pm$ 0.000 & 0.942 $\pm$ 0.000 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.5\linewidth]{figures/fig2_theorem_validation.pdf}
\caption{Theorem validation: predicted vs observed coverage-only AUROC.}
\label{fig:theorem}
\end{figure}

\section{Dataset Statistics}
\label{app:datasets}

\begin{table}[h]
\centering
\caption{Dataset statistics.}
\begin{tabular}{lccc}
\toprule
Dataset & Entities & Relations & Training Triples \\
\midrule
WN18RR & 40,943 & 11 & 86,835 \\
FB15k-237 & 14,541 & 237 & 272,115 \\
YAGO3-10 & 123,161 & 37 & 1,079,040 \\
\bottomrule
\end{tabular}
\end{table}

\section{Coverage Statistics}
\label{app:coverage}

\begin{table}[h]
\centering
\caption{Coverage statistics for ID test triples.}
\begin{tabular}{lcccc}
\toprule
Dataset & $p_h$ & $p_t$ & $P(\text{both})$ & $s_r$ (avg) \\
\midrule
WN18RR & 0.636 & 0.885 & 0.542 & 0.834 \\
FB15k-237 & 0.763 & 0.905 & 0.685 & 0.960 \\
YAGO3-10 & 0.71* & 0.89* & 0.63* & 0.92* \\
\bottomrule
\end{tabular}
\end{table}

*YAGO3-10 values estimated from coverage-only AUROC (0.760) using Theorem~\ref{thm:coverage_auroc}; exact computation infeasible due to dataset size (123K entities).

Note: $p_h < p_t$ because test triples often query rare heads against common tails (standard evaluation protocol).
